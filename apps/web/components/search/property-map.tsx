export function PropertyMap() {
  return (
    <section aria-label="Map view" className="grid min-h-[360px] place-items-center border border-dashed border-line bg-paper p-6 text-center">
      <div><p className="text-sm font-semibold text-ink">Explore homes on a map</p><p className="mt-2 max-w-sm text-sm leading-6 text-muted">A map view will be available when a location provider is configured.</p></div>
    </section>
  );
}
